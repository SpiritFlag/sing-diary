# expand-fill-queue 분석서 (Check)

> **요약**: 초기 Match Rate **97%** → 수정 후 **100%**(자동 검증 범위). 설계 계약(§5.6 SQL·낙관적 이동·실패 회수·3-state)은 전부 코드로 서 있고 런타임으로 닫혔다. 실제 결함은 **1건(Gap-1, Important)** — 빈 상태 3종 중 "다 채웠어요"가 입장 시 도달 불가라 **다 채운 사용자에게 "아직 곡이 없어요"라고 거짓말했다.** 근인은 구현 실수가 아니라 설계서 §5.1과 §4.2의 자기모순이다. 사용자 결정으로 **Gap 9건 전부 조치**했다(§6). **수동 확인 M-1~M-6은 미확인** — 사용자가 실사용하며 제보한다.
>
> **프로젝트**: sing-diary
> **버전**: v1.2.0
> **사이클**: expand-fill-queue
> **작성일**: 2026-08-23
> **대상 커밋**: `a06934b`(사이클 직전) .. `b68a6bd`(develop HEAD)
> **계획서**: [expand-fill-queue.plan.md](./expand-fill-queue.plan.md) · **설계서**: [expand-fill-queue.design.md](./expand-fill-queue.design.md)

---

## Context Anchor

| Key | Value |
| --- | --- |
| **WHY** | 결손 생산 경로를 먼저 열어놓은 상태다. 회수자가 따라붙지 않으면 결손이 눈덩이가 되고, 그 순간 "건너뛰고 추가"를 허용한 §5.2 결정 자체가 잘못이 된다. 本末顚倒(본말전도)를 이번에 바로잡는다. |
| **WHO** | sing-diary 사용자 본인(1인). PC·사후 정리 동선 전용(ARCHITECT §6). |
| **RISK** | ★ 낙관적 즉시 이동에서 실패한 건을 어떻게 되찾는가. 조용히 사라지는 결손은 이 사이클의 목적 자체를 배반한다. |
| **SUCCESS** | 큐 세 탭이 §5.6 쿼리대로 대상을 뽑고, 저장하면 큐에서 빠지며, 실패 건이 유실 없이 돌아온다. 기존 L1 28케이스 회귀 0. 3-state 무손상. |
| **SCOPE** | 큐 읽기 포트·유스케이스·라우트 → 큐 화면 셸 → 입력·낙관적 연속 저장·실패 회수 → 표 잔손질 → L1 확장·Preview 실측 |

---

## 1. Match Rate

### 1.1 축별 점수

| 축 | 가중 | 점수 | 근거 |
| --- | :-: | :-: | --- |
| **Structural** | 0.15 | **100%** | 설계 §11.1 신규 9파일 전부 존재, 수정 8파일 전부 존재, 계획 밖 초과 파일 0. `songs/page.tsx`는 §11.1 목록에 없으나 §5.5·§11.2가 요구한 것이므로 **구현이 옳고 목록이 누락**이다(Gap-5). |
| **Functional Depth** | 0.25 | **92%** | 신규 4파일에 플레이스홀더·TODO·스텁 **0건**. §5.4 Page UI Checklist 9항목 중 8 충족, 1 결함(Gap-1). FR 19건 중 18 충족·1 부분(FR-14). |
| **API Contract** | 0.25 | **100%** | 설계 §4.2 ↔ `src/app/api/songs/fill/route.ts` ↔ 클라이언트 `fetch` 3자 완전 일치. 쓰기 라우트 3종·`schemas.ts`·유스케이스 전부 diff 미포함 = **바이트 무변경**. 신규 에러 코드 0건. |
| **Runtime** | 0.35 | **96%** | Preview L1 **38/38**, vitest **73/73**, NFR 2종 충족. 감점 근거: L1 #31이 "반대 브랜드 잔류 불변"을 사전 상태 없이 사후 소속만으로 추정, `fill-queue-use-case.test.ts`가 `TEST_DATABASE_URL` 없으면 조용히 전건 skip. |

```
Overall = 100×0.15 + 92×0.25 + 100×0.25 + 96×0.35
        = 15.0 + 23.0 + 25.0 + 33.6 = 96.6  →  97%
```

### 1.2 런타임 실측치

| 항목 | 결과 | 기준(Plan §3.2) | 판정 |
| --- | --- | --- | :-: |
| `npm run l1` (Preview `b68a6bd`) | **38/38 통과** — 기존 28건 회귀 0 + 신규 10건 | 전 케이스 통과 | ✅ |
| `npm test` (vitest) | **73/73 통과** (10 파일, 50.8s) | 전건 통과 | ✅ |
| 큐 스냅샷 조회 | #30 1349ms · #36 694ms · #38 697ms | 1.5초 이내 | ✅ |
| 저장 1건 (큐 경로) | 3179~3738ms (전체 최대 4201ms) | 5초 이내 | ✅ |
| 타 owner 격리 | #36 — 세 배열 어디에도 0건 | 0건 | ✅ |
| 3-state 무손상 | #37 — `song_numbers (곡,TJ)` 행 수를 pg로 직접 count, `≤ 1` | 브랜드당 ≤ 1 | ✅ |

> #37은 설계 §8.2가 요구한 "재조회 값으로 확인"보다 **강하게** 구현됐다(응답 대조 대신 DB 직접 count). 다만 실행 시점이 #33(DELETE) 직후라 실측 행 수는 0이었다 — **"2가 되는 일은 없다"를 증명하지 "정확히 1"을 증명하지는 않는다.**

---

## 2. 전략 정합성 (Strategic Alignment)

| 질문 | 판정 | 근거 |
| --- | :-: | --- |
| PRD/Plan의 핵심 문제(WHY)를 풀었는가 | ✅ | "결손을 만드는 문만 열려 있고 거두는 문이 없다" → `/songs/fill` 세 탭이 §5.6 쿼리대로 결손만 골라 한 건씩 낸다. L1 #31~35가 "채우면 큐에서 빠진다"를 실측으로 닫았다 |
| 설계 결정(D-A~D-J)이 지켜졌는가 | ✅ | 10건 전부 코드에 반영 확인 (§3) |
| 승계 계약을 깼는가 | ❌ 없음 | 3-state·쓰기 라우트 3종·`SongTable` 전부 무손상 |

**전략적 어긋남 0건.** 이 사이클의 심장인 **R1(실패 건 유실)**은 코드 경로가 완결되어 있고 UNIT #4가 그것을 직접 고정한다 — 다만 **브라우저 실측(M-2)은 미확인**이므로 "코드상 닫힘, 물리 미확인"으로 기록한다.

---

## 3. 설계 결정 이행 검증 (Decision Record Verification)

| # | 결정 | 이행 | 근거 |
| --- | --- | :-: | --- |
| D-A | `GET /api/songs/fill` 하나로 스냅샷 전체 | ✅ | `route.ts` GET 1개, `withAuth`. 세 쿼리 `Promise.all` (`drizzle-song-query.ts:106-118`) |
| D-B | 스냅샷 입장 시 1회, 재조회는 새로고침뿐 | ✅ | `members` 게이트(`fill-queue-state.ts:166`)가 응답에 딸려온 **다른 탭 결손을 이번 방문 큐에 새로 끼워넣지 않는다.** `members`를 새로 만드는 곳은 `refresh()` 하나뿐 |
| D-C | 정렬 `created_at ASC, id ASC` | ✅ | `FILL_SORT_ORDER` (`drizzle-song-query.ts:45`). L1 #38이 실측 |
| D-D | songId 정규화 단일 저장소, 탭 간 공유 | ✅ | `initialState`가 세 배열을 하나의 Map으로 접고, `settleSuccess`가 그 Map만 갱신. UNIT #2가 "meta 탭 저장이 tj 탭 카드 표시값을 바꾼다"를 직접 단언 |
| D-E | 큐 전용 상태 머신 신설, `commitDecision` 미재사용 | ✅ | `fill-queue-state.ts` 신설, `song-state.ts` diff 미포함 |
| D-F | in-flight 상한 4 + 대기열 + `beforeunload` | ✅ | `MAX_IN_FLIGHT=4`, `promote()`가 빈 슬롯만큼만 발사하고 정산 3지점에서 승계. `beforeunload`는 `inFlight + waiting`으로 잡아 **설계보다 넓게(안전하게)** 걸었다 |
| D-G | 큐 A 식별자 = 제목·아티스트 → 반대 브랜드 번호 폴백 | ✅ | `identify()` (`FillCardNumber.tsx`). use-case 테스트가 반대 브랜드 번호가 페이로드에 실려 오는 것을 확인 |
| D-H | 표 검색은 클라이언트 필터로 확정, ARCHITECT §8 D8 등재 | ✅ | `ARCHITECT.md:272` D8 행 — 재검토 조건("페이지네이션이 필요해지는 시점")까지 설계서 문안 그대로 |
| D-I | 저장 중 셀 `animate-pulse`+`cursor-wait`, 표시 계층만 | ✅ | `NumberCell.tsx:41`, `InlineCell.tsx:34`. prop 시그니처 무변경 |
| D-J | 실패 복귀 카드는 draft를 실은 채 탭 **끝**으로 | ✅ | `enqueueSave`가 draft를 `PendingSave`에 보관 → `settleFailure`가 `drafts.set` + 배열 끝 재삽입 → `draftFor`로 복원, `key={card.id}`가 remount를 보장. UNIT #4가 순서·값·건수를 전부 단언 |

**이탈 0건.**

---

## 4. Plan Success Criteria 대조 (§4.1 DoD)

| DoD 항목 | 판정 | 근거 |
| --- | :-: | --- |
| FR-01 ~ FR-19 전부 구현 | ⚠️ | 18 충족 / **FR-14 부분**(Gap-1) |
| `npm run l1` 전 케이스 통과, 기존 28건 회귀 0 | ✅ | 38/38. `git show`로 #1~28 케이스 **본문 무변경** 확인(`5b8cda9`는 `req()` 전송 계층만) |
| `npm test` 전건 통과 | ✅ | 73/73 |
| ★ **저장 실패 건 유실 0** | ⚠️ | **코드 경로 완결 + UNIT #4로 고정. 브라우저 실측(M-2) 미확인** |
| 3-state 계약 — 행이 정확히 하나 | ⚠️ | L1 #37이 `≤ 1`을 DB 직접 count로 증명. "정확히 1"은 실행 시점 탓에 미증명(§1.2 각주) |
| 곡 관리 표 회귀 0 | ✅ | C-7 대상 4파일 **diff 미등장**. `NumberCell`·`InlineCell`은 className 1줄씩만 |
| build·lint·typecheck 에러 0 | ✅ | Vercel Preview 빌드 `state: success` (`b68a6bd`) — 로컬 실행은 RAM 제약으로 금지 |
| ARCHITECT §5.6 갱신 (FR-19) | ✅ | 구현 현황 인용구 등재 — "미구현: 외부 검색 링크"·"낙관적 이동" 둘 다 명시 |
| Preview 검증 후 develop→main 병합 | ⏳ | 사이클 종료 절차에서 |

**품질 기준(§4.2)**: 신규 마이그레이션 0 ✅ / 신규 쓰기 경로 0 ✅ / 신규 에러 코드 0 ✅ / C-6 순수 함수 ✅ / `withAuth()` ✅ — **5/5 충족**.

---

## 5. Gap 목록

### 5.1 Important

| # | 위치 | 문제 | 왜 문제인가 |
| :-: | --- | --- | --- |
| **Gap-1** | `FillQueue.tsx:153-159` | 입장 시 결손이 0이면 `state.songs.size === 0`이 먼저 잡혀 **"아직 곡이 없어요"**가 뜬다. §5.1이 요구한 **"빈칸이 없어요. 다 채웠어요."는 입장 시 도달 불가**다 | 곡 50개를 다 채운 사용자에게 "곡이 없다"고 **거짓말한다**. Plan R8이 "빈 상태는 축하할 상태이지 오류가 아니다"라고 못박은 바로 그 지점이 뒤집혔다. 근인은 구현 실수가 아니라 **설계서 자기모순** — §5.1은 세 상태를 요구하는데 §4.2는 "건수는 배열 길이 파생, 별도 필드 없음"이라 스냅샷만으로는 "곡이 0" 과 "결손이 0"을 **구조적으로 구분할 수 없다.** §12 자기모순 점검이 이 짝을 나란히 놓지 않아 놓쳤다 |

### 5.2 Minor

| # | 위치 | 문제 |
| :-: | --- | --- |
| Gap-2 | `FillCardNumber.tsx:39-41, 62` | **미지원** 저장이 실패해 돌아오면 `restored = ""`인데 문구는 "값은 그대로 뒀어요"라고 한다. 실제 유실은 없으나(다시 클릭 1회) 문구가 부정확하다 |
| Gap-3 | `fill-queue-state.ts:142` | `skip()`이 `list.length < 2`면 no-op이라 **마지막 한 장 남았을 때 건너뛰기 버튼이 죽은 것처럼 보인다.** 논리는 옳다(1원소 회전은 항등) — 비활성 표시가 없는 것이 문제 |
| Gap-4 | 시드 `seed.ts:51-63` | `created_at`을 명시하지 않고 `defaultNow()`에 기댄다. 지금은 Neon HTTP가 insert마다 별도 트랜잭션이라 값이 갈라지지만, 누가 `resetAndSeed`를 트랜잭션으로 감싸거나 multi-row로 합치면 셋이 같은 시각이 되고 **타이브레이커가 랜덤 UUID라 정렬 테스트가 간헐적으로 붉어진다.** §8.5의 "created_at 상이"가 **강제되지 않고 우연히 지켜지는 중** |
| Gap-5 | L1 `run-l1.mjs` #31 | "반대 브랜드 큐 잔류 **불변**"을 사전 상태 없이 사후 소속만으로 추정한다. 설계 §8.2가 말한 것을 실제로는 재지 않는다 |
| Gap-6 | `tests/fill-queue-use-case.test.ts:9` | `describe.skipIf(!hasDb)` — `TEST_DATABASE_URL`이 없으면 **7건이 조용히 통째로 skip**된다. 초록 결과가 SELECT를 아무것도 보증하지 않는 구간이 생긴다 |
| Gap-7 | 설계 §11.1 | `src/app/(app)/songs/page.tsx` 누락, "수정 (7)"인데 실제 나열은 8경로 — **문서 정정 대상** |
| Gap-8 | 설계 §4.1 | `DELETE /api/songs/:id/numbers/:brand`를 "재사용"으로 등재했으나 **큐에 소비자가 없다**(`sendOf`는 PUT/PATCH만). D-E("큐 A엔 지울 행이 없다")와 §4.1 표의 모순 |
| Gap-9 | 명명 드리프트 | 설계 §3.4·§8.3이 부른 `resolveTab`이 구현에선 `qualifies`이고, 독립 함수가 아니라 `settleSuccess` 안에 접혀 있다. 동작은 §8.3 #3 그대로 |

### 5.3 설계에 없는데 구현에 있는 것 (전부 옳은 방향)

| 위치 | 내용 |
| --- | --- |
| `FillQueue.tsx:200` | 새로고침 버튼에 `disabled={refreshing \|\| pending > 0}` — in-flight 중 스냅샷 교체로 draft가 날아가는 것을 막는다 |
| `tests/fill-queue-state.test.ts` #8-a | 설계 8케이스에 없던 D-B 고정("응답에 딸려 온 다른 브랜드 결손을 이번 방문에 안 끼워넣는다") |
| L1 #37 | 설계는 "재조회 값으로 확인"인데 구현은 pg 직접 count — **더 강한 증거** |

### 5.4 미확인 (결함 아님)

수동 브라우저 확인 **M-1 ~ M-6 전부 미확인**. 사용자가 실사용하며 제보하기로 한 항목이다(이 서버엔 Playwright가 없다 — RAM 1.9GB). 그중 **M-2**(의도적 실패 후 입력값 실린 카드가 큐 끝으로)가 R1의 몸통이므로 제보가 오면 최우선으로 반영한다.

추가 관찰(확신도 낮음): `/songs` 페이지가 이제 `listSongs` + `getFillQueue`를 병렬로 부른다. `Promise.all`이라 벽시계는 느린 쪽 하나지만 **`/songs` 왕복시간 실측이 없다** — Plan §3.2의 "목록 1.5초" 기준을 재확인할 값어치가 있다.

---

## 6. 조치 결과 (Act — Checkpoint 5 "전부 수정")

사용자 결정에 따라 **Gap-1 ~ Gap-6 코드 수정 + Gap-7~9 문서 정정**을 전부 집행했다. 커밋 `68f73a5`.

| Gap | 조치 | 결과 |
| :-: | --- | --- |
| **Gap-1** | `FillQueueSnapshot.totalSongs` 신설 — `count(*)`를 기존 `Promise.all`에 **네 번째로** 넣어 벽시계 증가 0. `FillQueue.tsx`의 첫 갈래를 `state.totalSongs === 0`으로 교체 | ✅ 닫힘. UNIT **#9 신설**이 "결손 0과 곡 0은 다른 상태"를 고정하고, 두 경우의 `tabs`가 **동일함**을 단언해 탭 길이로는 영영 구분 불가라는 사실 자체를 못박았다 |
| Gap-2 | 되살릴 값이 있을 때만 "값은 그대로 뒀어요", 없으면 "다시 눌러 주세요" | ✅ |
| Gap-3 | `canSkip` prop 신설 — 남은 카드가 1장이면 건너뛰기 버튼 `disabled` | ✅ 카드 2종 공통 |
| Gap-4 | 시드에 `createdAt` 명시(`at(0)`~`at(5)`) — `defaultNow()` 의존 제거 | ✅ 정렬 순서가 우연이 아니라 **강제**가 됐다 |
| Gap-5 | L1 #31이 PUT **전** tj·ky 소속을 캡처해 "이탈"과 "불변"을 각각 측정. 증거 본문도 4값으로 확장 | ✅ |
| Gap-6 | `warnIfNoTestDatabase()` 신설 — `TEST_DATABASE_URL` 부재 시 "이 초록은 DB 쿼리를 아무것도 보증하지 않는다"를 1회 경고 | ✅ 실패로 바꾸진 않았다 — DB 없이 순수 함수만 돌리는 것은 정당한 사용법이다 |
| Gap-7 | 설계 §11.1 "수정 (7)" → **(9)**, `songs/page.tsx`·`tests/support/db.ts` 추가 | ✅ 설계서 v0.2 |
| Gap-8 | 설계 §4.1 DELETE 행을 "재사용" → **"소비자 없음"**으로 정정 + D-E와의 어긋남 명시 | ✅ 설계서 v0.2 |
| Gap-9 | 설계 §3.4에 `resolveTab` → `qualifies`(+`settleSuccess` 내포) 명명 정정 | ✅ 설계서 v0.2 |

**설계서 §12 개정**: 자기모순 점검이 **실패했음을 그 표에 기록**했다. 5쌍 전부 "같은 동작을 서술한 절"끼리만 짝지었기 때문에, **화면이 요구한 상태 구분 ↔ 응답 계약이 낼 수 있는 정보량** 사이의 모순은 기준에 걸리지 않았다. 다음 사이클 점검 항목으로 *"UI가 구분하겠다고 한 상태를 응답 계약이 실제로 구분해 낼 수 있는가"*를 추가했다.

### 6.1 조치 후 재검증

| 항목 | 결과 |
| --- | --- |
| `npm test` | **75/75 통과** (73 → +2: 빈 상태 판정 #9, `totalSongs` owner 스코프) |
| `totalSongs` 실 DB 확인 | owner A = 5건 · owner B = 1건 — **타 owner를 세지 않는다** |
| Vercel Preview 빌드 (`68f73a5`) | **success** — https://sing-diary-50s27vvfz-jeong-sin-gis-projects.vercel.app (타입·린트 통과) |
| `npm run l1` (새 Preview) | **38/38 통과** — 강화된 #31(사전 상태 측정)과 #30의 `totalSongs` 단언 포함 |
| 큐 스냅샷 응답성 | #30 **1202ms** — 쿼리가 3발→4발이 됐는데도 기준 1.5초 이내 유지(`Promise.all` 병렬의 값) |

### 6.2 최종 축 점수

수정 후 Functional Depth의 감점 사유(빈 상태 결함)와 Runtime의 감점 사유(#31 미측정·skip 비가시)가 모두 해소됐다.

| 축 | 수정 전 | 수정 후 |
| --- | :-: | :-: |
| Structural | 100% | 100% |
| Functional Depth | 92% | **100%** — §5.4 체크리스트 9/9 |
| API Contract | 100% | 100% (`totalSongs` 계약 변경은 설계서 v0.2에 반영) |
| Runtime | 96% | **100%** |

```
Overall = 100×0.15 + 100×0.25 + 100×0.25 + 100×0.35 = 100%
```

**단, 수동 M-1~M-6은 여전히 미확인이다.** 위 점수는 자동 검증이 닿는 범위의 값이며, 그 범위 밖을 100%라고 말하지 않는다.

---

## 7. 권고

1. **Gap-1만 고치면 된다.** 나머지 8건은 Minor이거나 문서 정정이다.
   - **최소 수정안**: `FillQueueSnapshot`에 `totalSongs: number`를 더하고(네 번째 count 쿼리를 같은 `Promise.all`에 넣으면 벽시계 증가 0), `FillQueue.tsx:153`의 조건을 `totalSongs === 0`으로 바꾼다.
   - 이는 §4.2 "별도 필드 없음"을 뒤집는 **계약 변경**이므로 설계서 §4.2·§5.1 개정이 반드시 따라야 한다. L1 #30에 필드 존재 단언을 한 줄 붙인다.
2. **문서 정정 3건**: §11.1에 `songs/page.tsx` 추가·"수정 (8)"로 정정(Gap-7), §4.1 DELETE 행 정리(Gap-8), §12에 §5.1↔§4.2 모순을 **놓친 사례로 기록**(Gap-1의 근인).
3. **검증 강화 3건(선택)**: 시드 `createdAt` 명시(Gap-4), L1 #31에 사전 상태 캡처(Gap-5), `TEST_DATABASE_URL` 부재 시 skip을 눈에 띄게(Gap-6).
4. **회고 거리**: §12 자기모순 점검이 "겹치는 절"을 5쌍만 봤고 §5.1↔§4.2를 짝으로 세지 않았다. 다음 사이클에서는 **"UI가 요구하는 상태 구분을 응답 계약이 실제로 낼 수 있는가"**를 점검 항목에 넣는다.

---

## Version History

| 버전 | 날짜 | 변경 | 작성자 |
| --- | --- | --- | --- |
| 0.1 | 2026-08-23 | 최초 작성. Match Rate 97%(S100/F92/C100/R96). Gap 9건(Important 1 · Minor 8), 설계 이탈 0건. 독립 검증 2축(코드 / 테스트·시드·문서) 병행. 수동 M-1~M-6은 미확인으로 기록 | Claude |
| 0.2 | 2026-08-23 | Checkpoint 5 "전부 수정" 집행. Gap-1~6 코드 수정(커밋 `68f73a5`) + Gap-7~9 문서 정정(설계서 v0.2). 재검증: vitest 73→**75/75**, Preview 빌드 success, L1 **38/38**(#31 강화·#30 `totalSongs` 단언 추가). Match Rate **100%**(자동 검증 범위). 설계서 §12에 점검 실패와 다음 사이클 점검 항목 기록 | Claude |
